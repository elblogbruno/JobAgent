{
  "name": "jobagent-browser-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Job Agent browser extension: one-click searches and one-click job import.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
